import 'package:flutter/material.dart';

class LoturaColorView extends StatelessWidget {
  const LoturaColorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

import 'package:flutter/material.dart';

class LoturaCheckBoxView extends StatelessWidget {
  const LoturaCheckBoxView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

import 'package:flutter/material.dart';

class LoturaImageView extends StatelessWidget {
  const LoturaImageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

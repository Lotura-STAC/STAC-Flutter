import 'package:flutter/material.dart';

class LoturaTextFieldView extends StatelessWidget {
  const LoturaTextFieldView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
